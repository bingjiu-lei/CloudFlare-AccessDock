{
  "name": "cloudflare-accessdock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply ACCESSDOCK_DB --local",
    "db:migrate": "wrangler d1 migrations apply ACCESSDOCK_DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}

